[project]
name = "lk-stt-cli"
version = "0.1.0"
description = "Transcribe audio files via LiveKit Inference STT"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents>=1.5.0",
    "aiofiles>=24.0",
    "python-dotenv>=1.0",
]
